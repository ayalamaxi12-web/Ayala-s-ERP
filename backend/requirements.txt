fastapi==0.115.0
uvicorn==0.30.6
requests==2.32.3
gspread==6.2.1
google-auth==2.35.0
python-dotenv==1.0.1
httpx==0.27.0

# Motor de Rentabilidad (backend/rentabilidad/) — Postgres en Railway + Alembic
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12
pytest==9.1.1

# Adaptador SQL de Táctica (SQL Server) — pymssql trae FreeTDS embebido,
# no requiere instalar un driver ODBC de sistema (más simple en Docker/Railway
# que pyodbc + msodbcsql).
pymssql==2.3.13

# Adaptador Excel de ECOM — lectura del reporte que hoy se descarga a mano.
openpyxl==3.1.5

# Requerido por FastAPI para recibir archivos/form-data (endpoints /cierres/ecom/excel).
python-multipart==0.0.32
